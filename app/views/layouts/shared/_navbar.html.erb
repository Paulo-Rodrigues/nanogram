  <nav>
    <div class="nav-wrapper">
      <%= link_to "Nanogram", root_path, class: 'brand-logo' %>
      <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
      <ul class="right hide-on-med-and-down">
        <% if account_signed_in? %>
          <li><%= link_to "Dashboard", dashboard_path %></li>
          <li> <%= link_to "Profile", profile_path(current_account.username) %></li>
          <li><%= link_to "New Post", new_post_path%></li>
          <li><%= link_to "Logout", destroy_account_session_path, method: :delete %>
        <% else %>
          <li><%= link_to "Register", new_account_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </nav>

  <ul class="sidenav" id="mobile-demo">
        <% if account_signed_in? %>
          <li> <%= link_to "Profile", "#"%></li>
          <li><%= link_to "New Post", new_post_path%></li>
          <li><%= link_to "Logout", destroy_account_session_path, method: :delete %>
        <% else %>
          <li><%= link_to "Register", new_account_registration_path %></li>
        <% end %>
  </ul>
